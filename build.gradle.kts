// Minimal top-level gradle file. Plugin versions are declared in settings (or use defaults).
buildscript {
    repositories {
        google()
        mavenCentral()
    }
}
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}